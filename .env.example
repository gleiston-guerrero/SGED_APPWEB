# ==============================================================
# SGED - Variables de Entorno (Plantilla de Configuración)
# Instrucciones:
# 1. Copie este archivo como .env: cp .env.example .env
# 2. Reemplace los valores marcados con 'cambiar_en_produccion' o 'changeme'.
# ==============================================================

# ==========================
# Base de Datos PostgreSQL
# ==========================
# Opción 1: Desarrollo Local / Docker Compose
# `make up` monta db/schema.sql + db/seed.sql en /docker-entrypoint-initdb.d/
# (solo corre en un volumen nuevo/vacío) y el backend arranca con
# FLYWAY_ENABLED=false porque el esquema ya está aplicado. Para correr el
# backend suelto (IDE / start-local.bat) contra un Postgres local sin ese
# volumen, usar en cambio FLYWAY_ENABLED=true más abajo: Flyway aplica
# V1..V15 solo, sin pasos manuales.
# DB_URL=jdbc:postgresql://postgres:5432/sged_db?prepareThreshold=0&preparedStatementCacheQueries=0
# DB_USER=postgres
# DB_PASSWORD=changeme

# Opción 2: Supabase (Transaction Pooler, puerto 6543 — uso normal de la app)
# NOTA: prepareThreshold=0 no es solo por PgBouncer — sin él, Hibernate 6 +
# pgjdbc pueden fallar con "could not determine data type of parameter" (o
# "cannot cast type bytea to date") en cualquier consulta con parámetros
# opcionales tipo "(:x IS NULL OR columna >= :x)" sobre columnas de fecha,
# también contra Postgres local sin PgBouncer de por medio. Por eso va en
# las dos opciones de arriba, no solo en Supabase.
DB_URL=jdbc:postgresql://aws-1-us-east-1.pooler.supabase.com:6543/postgres?prepareThreshold=0&preparedStatementCacheQueries=0

DB_USER=postgres.tu_id_de_proyecto
DB_PASSWORD=tu_contrasena_de_supabase_aqui

# Solo con Supabase + FLYWAY_ENABLED=true: el Transaction Pooler (6543) no
# sostiene los advisory locks de sesión que Flyway necesita para migrar.
# Apuntar estas tres variables al Session Pooler (mismo host, puerto 5432)
# o a la conexión directa, dejando DB_URL de arriba en 6543 para la app.
# Sin esto, Flyway usa DB_URL para todo (comportamiento de Docker/local).
# FLYWAY_DB_URL=jdbc:postgresql://aws-1-us-east-1.pooler.supabase.com:5432/postgres
# FLYWAY_DB_USER=postgres.tu_id_de_proyecto
# FLYWAY_DB_PASSWORD=tu_contrasena_de_supabase_aqui

# ==========================
# Usuario Inicial Administrador (Seeding)
# ==========================
# A diferencia de JWT_SECRET, esta SI es la credencial real de produccion,
# a proposito: el Bloque A.4.1 exige un usuario demo con credenciales
# publicadas para el tribunal, y README.md ya publica admin/sged2026 (y
# el resto de cuentas de seed) como cuenta de demostracion. Es el unico
# secreto de esta lista que no se rota (ver docs/despliegue/DEPLOYMENT.md,
# tabla de variables de produccion). Antes de 2026-09-17 este archivo
# traia un valor distinto (Admin123456*) con aspecto de contrasena real
# inventada, sin relacion con la credencial de demostracion realmente
# usada -- inconsistencia corregida (P11, evaluacion integral 17-sep).
USUARIO_ADMIN=admin
CONTRASENA_ADMIN=sged2026

# ==========================
# Redis
# ==========================
# Usar 'redis' si la app corre dentro de Docker Compose.
# Usar 'localhost' si ejecutas la app localmente con 'mvn spring-boot:run'.
REDIS_HOST=localhost
REDIS_PORT=6379

# ==========================
# JWT (RFC 7519)
# ==========================
# Mínimo 32 caracteres. CAMBIAR obligatoriamente en entornos de producción.
#
# DECLARACIÓN SOBRE EL VALOR ANTERIOR (P11, 2026-09-16): hasta el commit
# 634537b este archivo traía el literal
# SGED_2026_SECRET_KEY_MUY_LARGA_Y_SEGURA_123456789. Ese valor fue siempre
# un marcador de desarrollo y NUNCA firmó un token real: en producción
# (render.yaml) JWT_SECRET se define con `generateValue: true`, es decir,
# Render genera un secreto aleatorio propio que jamás se escribió en el
# repositorio ni en ningún commit. El literal quedó solo en el historial
# de git como testimonio del defecto ya corregido.
#
# DECLARACIÓN SOBRE UN SEGUNDO VALOR HISTÓRICO (P11, evaluación integral
# 2026-09-17): antes de esa misma corrección hubo otro literal, más viejo,
# que esta declaración no cubría: SGED_SUPER_SECRET_KEY_2026_SEGURA_MINIMO_32_CARACTERES,
# como valor por defecto de Spring (`${JWT_SECRET:...}`) en
# backend/src/main/resources/application.yml y en docker-compose.yml,
# desde el primer commit del backend (74bb44f, 2026-06-16) hasta que se
# quitó el fallback en e9e5ba7 (2026-07-23) — misma naturaleza que el
# valor de arriba (marcador de desarrollo, nunca secreto de producción,
# nunca leído salvo que JWT_SECRET no estuviera definido) y mismo defecto:
# quedó solo en el historial de git, ya no en ningún archivo versionado.
JWT_SECRET=CAMBIAR_EN_PRODUCCION_min_32_caracteres_aleatorios
JWT_EXPIRATION_MS=3600000
JWT_REFRESH_EXPIRATION_MS=604800000
JWT_ISSUER=sged-backend
JWT_AUDIENCE=sged-frontend

# ==========================
# Cookies y Seguridad
# ==========================
# Marca las cookies de sesion como Secure. Dejarlo en true: los navegadores
# tratan http://localhost como origen confiable y aceptan cookies Secure ahi,
# asi que NO hace falta ponerlo en false para desarrollar.
#
# El unico caso que se rompe es entrar por HTTP plano desde otro dispositivo
# -por ejemplo el celular contra http://192.168.x.x:4200 para probar el QR-.
# Para eso conviene usar la URL publica con HTTPS (ver docs/despliegue/), que
# ademas es mas parecida al uso real.
COOKIE_SECURE=true

# ==========================
# CORS
# ==========================
# Origenes permitidos para llamar a /api/**. Admite "*" en un segmento
# (setAllowedOriginPatterns, no setAllowedOrigins), asi que se puede cubrir
# una subred completa en vez de fijar una IP exacta que cambia por DHCP.
# Para probar desde el celular en la misma red WiFi, agrega tu propia IP
# de LAN (ver `ipconfig` / `ip addr`) sin quitar los localhost por defecto,
# por ejemplo: ...,https://192.168.1.*:8443,http://192.168.1.*:4200
#
# El frontend en produccion vive en un dominio fijo y conocido
# (docs/despliegue/render.md); se declara asi, sin comodin, en vez de
# https://*.onrender.com, que habilitaria credenciales cruzadas desde
# cualquier subdominio de Render, incluidos los de terceros.
#
# El tunel de Cloudflare (docs/despliegue/cloudflare-tunnel.md) es para
# demostraciones puntuales, con una subdomain aleatoria distinta cada vez:
# no se declara aqui. Si haces falta mostrar la app por ese tunel, agrega
# https://*.trycloudflare.com SOLO en tu .env local mientras dure la
# demostracion, y quitalo despues.
CORS_ALLOWED_ORIGIN_PATTERNS=http://localhost:4200,http://localhost:80,http://127.0.0.1:4200,http://localhost:5173,https://sged-frontend-jofa.onrender.com

# ==========================
# Swagger / OpenAPI (/api/docs)
# ==========================
# Comoda en desarrollo, pero la version de Swagger UI que trae springdoc
# 2.3.0 empaqueta una copia vieja de DOMPurify (3.0.6) con una vulnerabilidad
# conocida -es lo que detecto el escaneo ZAP-. En vez de esperar a que
# springdoc actualice su bundle, se apaga en el ambiente publico: ahi nadie
# necesita la UI interactiva, alcanza con los DTO documentados en el codigo.
# En Render, esta variable se define en false para el servicio de backend.
SPRINGDOC_ENABLED=true

# ==========================
# Rate Limiting Login (OWASP A07)
# ==========================
LOGIN_MAX_INTENTOS=5
LOGIN_VENTANA_MINUTOS=15

# ==========================
# Caché y Migraciones
# ==========================
# TTL externo de caché en segundos
CACHE_TTL_SECONDS=60

# Habilitar Flyway si se gestionan migraciones de BD desde la app
FLYWAY_ENABLED=true

# ==========================
# Generación de texto con IA
# ==========================
# El sistema funciona completo sin ella: si el modelo no responde, la
# evaluación se guarda igual y el comentario simplemente no aparece.
#
# Qué proveedor genera el texto: 'openai' o 'gemini'. Solo se crea el bean
# del elegido, así que cambiar de proveedor no toca ni una línea del dominio.
# Si no se define, cae a 'gemini', que sin clave queda no disponible.
IA_PROVEEDOR=openai

# --- proveedor 'openai' ---------------------------------------------------
# NUNCA committear la clave real: va en .env, que está en .gitignore.
# Se envía en la cabecera Authorization, nunca en la URL: una URL queda
# escrita en los registros del servidor y del proxy.
OPENAI_API_KEY=

# La misma implementación sirve para cualquier API compatible con OpenAI:
#   OpenAI      https://api.openai.com/v1
#   Groq        https://api.groq.com/openai/v1
#   OpenRouter  https://openrouter.ai/api/v1
#   NVIDIA NIM  https://integrate.api.nvidia.com/v1
OPENAI_BASE_URL=https://api.groq.com/openai/v1
OPENAI_MODELO=openai/gpt-oss-120b

# Reintentos ante un fallo transitorio. Un rechazo 4xx -clave inválida,
# cuota agotada- no se reintenta: reintentarlo no lo va a arreglar.
IA_REINTENTOS=1

# --- proveedor 'gemini' ---------------------------------------------------
# Solo aplica si IA_PROVEEDOR=gemini.
IA_HABILITADO=false

# Clave de Google AI Studio. NUNCA committear la real: va en .env, que está
# en .gitignore. Se envía en la cabecera x-goog-api-key, no en la URL.
GEMINI_API_KEY=

# Verificar en ai.google.dev qué modelo tiene nivel gratuito vigente antes
# de cambiarlo: solo las variantes Flash y Flash-Lite lo tienen.
# gemini-2.0-flash y gemini-2.0-flash-lite ya NO tienen nivel gratuito para
# keys nuevas (confirmado 2026-08-07: 429 RESOURCE_EXHAUSTED, limit 0, con
# tres keys distintas en cuentas distintas). gemini-flash-latest si funciona.
GEMINI_MODELO=gemini-flash-latest

# Timeout corto, pero con margen: el entrenador califica desde el celular
# en la cancha, y los modelos "-latest" actuales piensan antes de
# responder (~5s medidos en la practica solo para esa parte). 8s quedaba
# demasiado ajustado y podia cortar la respuesta en una red mas lenta.
IA_TIMEOUT_SEGUNDOS=15

# ==========================
# Correo saliente (recuperación de contraseña, RF-37 / RNF-15)
# ==========================
# El sistema funciona completo sin esto: con MAIL_ENABLED=false (por
# defecto) la solicitud de recuperación responde igual y el enlace queda
# escrito en la bitácora del backend (LoggingPasswordResetMailer). Así
# `make up` y la CI no necesitan ninguna credencial de correo.
#
# En producción, poner MAIL_ENABLED=true y completar las cinco variables.
# Con Gmail: activar la Verificación en 2 pasos y generar una "Contraseña
# de aplicación" en https://myaccount.google.com/apppasswords (16 letras,
# sin espacios). NUNCA committear MAIL_PASSWORD: va en .env (.gitignore) o
# como secreto en el panel de Render.
MAIL_ENABLED=false
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=
MAIL_PASSWORD=
MAIL_FROM=

# Base del enlace que recibe el usuario (se le agrega ?token=...). Debe
# apuntar al frontend público; en local queda el valor por defecto.
MAIL_RESET_URL_BASE=https://localhost:8443/#/restablecer

# Minutos de vigencia del enlace de un solo uso.
MAIL_RESET_TTL=30
